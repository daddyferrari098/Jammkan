{% extends 'base.html.twig' %}

{% block title %}Bienvenue - {{ parent() }}{% endblock %}

{% block body %}
    {% if app.user %}
        <div class="row mb-4">
            <div class="col">
                <h1>Mes tableaux récents</h1>
            </div>
            <div class="col-auto">
                <a href="{{ path('app_board_new') }}" class="btn btn-primary">
                    <i class="fas fa-plus me-1"></i> Créer un tableau
                </a>
            </div>
        </div>

        {% if boards is empty %}
            <div class="alert alert-info">
                <p>Vous n'avez pas encore créé de tableau.</p>
                <a href="{{ path('app_board_new') }}" class="btn btn-primary mt-2">
                    <i class="fas fa-plus me-1"></i> Créer mon premier tableau
                </a>
            </div>
        {% else %}
            <div class="row row-cols-1 row-cols-md-2 row-cols-xl-4 g-4">
                {% for board in boards %}
                    <div class="col">
                        <a href="{{ path('app_board_show', {'id': board.id}) }}" class="text-decoration-none">
                            <div class="board-card p-3 d-flex flex-column h-100">
                                <h3 class="h5">{{ board.name }}</h3>
                                {% if board.description %}
                                    <p class="small text-white-50 mb-0">{{ board.description|u.truncate(50, '...') }}</p>
                                {% endif %}
                            </div>
                        </a>
                    </div>
                {% endfor %}
                <div class="col">
                    <a href="{{ path('app_board_new') }}" class="text-decoration-none">
                        <div class="board-card create-board p-3 d-flex flex-column justify-content-center align-items-center h-100">
                            <i class="fas fa-plus fa-2x mb-2"></i>
                            <h3 class="h5">Créer un tableau</h3>
                        </div>
                    </a>
                </div>
            </div>
        {% endif %}
    {% else %}
        <div class="py-5 text-center">
            <h1 class="display-4 mb-4">Bienvenue sur Jammkan</h1>
            <p class="lead mb-4">Organisez vos projets, gérez vos tâches et collaborez efficacement.</p>
            <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
                <a href="{{ path('app_register') }}" class="btn btn-primary btn-lg px-4 gap-3">S'inscrire</a>
                <a href="{{ path('app_login') }}" class="btn btn-outline-secondary btn-lg px-4">Se connecter</a>
            </div>
        </div>
        
        <div class="row mt-5">
            <div class="col-md-4 mb-4">
                <div class="card h-100">
                    <div class="card-body text-center">
                        <i class="fas fa-list-check fa-3x mb-3 text-primary"></i>
                        <h3>Organisez vos tâches</h3>
                        <p>Créez des tableaux, des listes et des cartes pour organiser vos projets de manière visuelle.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card h-100">
                    <div class="card-body text-center">
                        <i class="fas fa-tags fa-3x mb-3 text-primary"></i>
                        <h3>Étiquettes colorées</h3>
                        <p>Utilisez des étiquettes colorées pour catégoriser vos tâches et les retrouver facilement.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card h-100">
                    <div class="card-body text-center">
                        <i class="fas fa-calendar-days fa-3x mb-3 text-primary"></i>
                        <h3>Dates d'échéance</h3>
                        <p>Ajoutez des dates d'échéance à vos cartes pour ne jamais manquer une deadline.</p>
                    </div>
                </div>
            </div>
        </div>
    {% endif %}
{% endblock %}
